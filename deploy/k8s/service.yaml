apiVersion: v1
kind: Service
metadata:
  name: healthcheck-api
  labels:
    app: healthcheck-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
  selector:
    app: healthcheck-api